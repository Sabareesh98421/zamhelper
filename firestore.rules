rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to the 'exams' collection
    match /exams/{examId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}